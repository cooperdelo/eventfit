# UI

## 🎨 Design Direction (for all pages)

**Overall vibe:**

- Clean, **college-lifestyle aesthetic**, like Notion × Pinterest × Apple.com
- Feels _human_, stylish, warm — _not_ techy or AI-coded.
- Neutral + natural tones: off-white (#FAFAFA), soft beige, muted navy, charcoal, denim, and minimal accent color (warm orange or forest green).
- No gradients, neon, or “glassmorphism.” Stick to **flat color blocks** and strong typography.

**Typography:**

- Heading: `Inter` or `Poppins` bold
- Body: `Inter` regular, light line spacing
- Use uppercase section headers and large hero typography.

**UI Rules:**

- Components have **2xl sharp corners**, **subtle shadows (not glowing)**, and **consistent padding (p-6 to p-8)**.
- Cards should use white backgrounds with thin gray borders (#E5E5E5).
- Lucide icons for visual anchors (each feature, step, or CTA).
- Hover states = soft color tint or scale animation.

### 🧩 Component Library Summary

You can mention to Cursor that all reusable UI should follow this structure:

- `Navbar` (white bg, border-b, sticky)
- `HeroSection`

- `FeatureCard`
- `StepCard`
- `TestimonialCard`
- `TeamCard`
- `Footer`
- `CallToActionSection`

Each card = `<Card className="border rounded-2xl p-6 shadow-sm bg-white hover:scale-[1.02] transition-all duration-200" />`

Icons from **lucide-react**.
